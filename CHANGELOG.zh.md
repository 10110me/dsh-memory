# 更新日志

dsh-memory 的所有重要变更。应用内更新器读取此文件，在更新横幅中展示新版内容。

## 1.1.5

- 修复节点仍然重叠：间距改为自适应——理想间距与最小间距都不超过「当前节点数在画布内能物理铺开的最大间距」，约束恒可满足（固定 85px 最小间距下 50 个节点根本放不进 660px 宽的画布）。画布加高到 620px，布局迭代次数增加。
- 实体数量控件增加反馈：画布下方常显统计行「实体 N · 关系 E · 共 T 个实体」；实体已全部显示时会明确提示（调大数量画面不会再变），避免误以为没生效；数量未变化时不再重复请求。
- 「重置」现在同时恢复会话级设置（实体数量回到默认 50）。
- 图谱请求附带防缓存参数，避免读到 HTTP 缓存的旧响应。

## 1.1.4

- 关系图工具栏重排为画布上方一行三个模块：缩放（−/百分比/+/重置）、实体数量、线条粗细。
- 修复缩放边界漂移：滚轮缩到最小或放到最大后继续滚动，画布不再平移。
- 缩放范围扩大：5% – 800%（原 20% – 400%）。
- 实体数量改为图谱工具栏上的会话级设置（默认 50，重新打开自动重置），不再是持久化配置项。
- 布局更宽松：节点最小间距 40 → 85px，理想间距下限 60 → 120px，斥力翻倍——节点不再挤在一起。

## 1.1.3

- 修复关系图塌缩：当两个实体名的初始位置哈希碰撞时，整团实体会缩成一个点——零距离斥力产生 NaN 并毒化该连通分量的所有坐标。现在初始位置改用更强的滚动哈希 + 黄金比例索引扰动；完全重合的节点对在计算斥力前先随机微移；布局过程中任何非有限坐标都会被重置。

## 1.1.2

- 更新日志本地化：同时拉取中文（CHANGELOG.zh.md）与英文日志，更新横幅根据浏览器/UI 语言自动选择显示哪种。
- 更新过程可视化：点「更新」后分两个真实阶段推进——「下载新版本」→「安装文件」，每阶段带转圈动画和步骤文字，不再是无反馈等待。

## 1.1.1

- 流程知识自动入库：会话中建立起可复用的构建/发布/部署等流程时，模型会以 knowledge 类型把确切命令与路径存进记忆（memory_save 描述新增触发器 + 系统提示第 5 条规则）。

## 1.1.0

- 更聪明的记忆工具使用：遇到 bug / 报错时先搜记忆（用报错关键词当查询）；修好非平凡 bug 后立即存 pitfall（症状 + 根因 + 有效修法）——通过工具描述与系统提示双重强化。
- memory_delete 支持按 query 一键删除最佳匹配（无需先查 id），并返回被删记忆的摘要供确认。
- 应用内更新系统：设置页顶部更新横幅（新版本号 + 更新日志），支持 忽略 / 忽略这个版本 / 更新 三种操作；可从 GitHub 自更新，全程只改动自身包目录内的文件。

## 1.0.0

- 首次发布。
- 记忆存储（pitfall / knowledge / note），持久化到工作区根目录 .dsh-memory-store.json。
- 模型辅助整理：保存时自动提取摘要、标签、实体。
- 可选 embedding 语义搜索（OpenAI 兼容 API）+ 内置倒排索引关键词搜索、输入防抖自动搜索。
- 实体关系图：缩放、平移、拖拽、点击高亮关联链、线宽调节、连通分量分组的力导向布局。
- 四个模型工具（memory_save / memory_search / memory_list / memory_delete）与系统提示引导。
- DSH Web 设置面板：配置 / 记忆库 / 关系图 三个页签。
